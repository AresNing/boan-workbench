# Boan Workbench 项目协作约定

## 核心产品与交互原则

**任务自动推进、无需管理会话。** 这是后续产品、UI 与交互迭代必须保持的原则。

- 以项目工作台承载目标、进展、待决定事项和成果；用户交代目标后，工作台自动安排执行、衔接与验证。
- 不要求用户新建、命名、整理或切换会话才能推进工作。执行会话属于内部实现，不作为主要导航对象。
- 项目切换应直接、轻量；切换视图不暂停其他项目任务，草稿与上下文按项目隔离。
- 默认突出需要用户处理的决定与成果。全局看板、执行过程、工作记录和任务背景保留明确入口，按需展开。
- 可以参考 T3 Code 等产品的紧凑导航、内容布局和面板交互；参考设计与以上原则冲突时，以工作台原则为准。
- 自动推进不等于自动批准权限：项目内命令默认在系统沙箱中自动执行，联网按范围申请授权，解除沙箱必须单次批准；保留暂停、撤销和成果验收边界。

## 执行逻辑规范

- 任务与会话关系按 [项目管理 Agent 规范 v1](docs/project-agent-spec.md)实施；当前支持任务依赖，主任务仍按项目串行；已接入输入与执行事件管理、受限辅助执行及独立子任务，管理操作已接入统一身份、版本与回执边界；统一事实上下文与交接现场观察已实现，验证方式与边界见 `docs/testing.md`。
- 要求先保存，接收要有证据；默认续接当前主会话，替换必须交接。交付与验收须对应最新要求版本。
- 不确定操作先核对，禁止自动重放旧审批；管理模型不得替用户验收。

## UI 与文案

- 使用简短、具体的功能名称和状态，避免口号、推广文案与重复解释。
- 常用操作直接可达：新建任务、补充要求、打开成果、确认完成；细节和高级设置按需展开。
- 浏览任务不自动改变输入的提交对象；补充要求、新建任务和项目切换不得丢失未发送草稿。
- 沿用桌面常见快捷键和导航逻辑，但不引入会话管理。

## 版本交付与切换

### 界面验证不得干扰用户

- 用户要求：直接操作软件时在另外一个桌面进行，不干扰当前桌面的正常使用。
- 优先使用隔离数据的后台自动化测试；不要为验证抢占用户当前应用的焦点、鼠标或键盘。
- 需要直接操作界面时使用独立桌面。若工具不能在不切换用户当前桌面的情况下操作，停止前台交互并说明限制，不把切换 macOS Space 当作无干扰的后台操作。
- 用户于 2026-09-07 再次明确要求后续自动打开最新版、退出旧版。版本交付时的正常退出和启动属于已授权动作，即使会显示新版窗口也无需再次确认；不要仅因可能显示窗口而暂缓版本切换。其他界面验证仍须遵守上述不干扰约束。

用户已于 2026-09-07 明确授权：每次迭代新版本后，自动关闭当前空闲的历史版本，打开新版，无需重复询问。

- 新版完成构建及相关验证后，先确认正在运行的旧版是否空闲。
- 旧版没有正在执行的任务时，正常退出旧版并打开本次验证通过的新版；若没有旧版运行，直接打开新版。检查版本、项目与登录状态恢复，优先采用后台只读方式。
- 若旧版仍在执行任务（包括等待命令审批的执行会话），或无法确认是否空闲，保留其运行，说明暂未切换；不要强制结束进程或中断任务。
- 保留项目记录、成果、草稿和登录配置；凭据不得进入源码、构建产物或外部上传。
- 构建产物只保留最近三个版本，沿用现有版本清理脚本及运行中版本保护。

此授权仅适用于本项目的本机版本切换，不包含对外发布、上传或其他应用的退出。
